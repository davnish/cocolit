apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
type: Opaque
stringData:
  postgres-password: {{ .Values.database.password }}
  postgres-user: {{ .Values.database.username }}
  postgres-database: {{ .Values.database.name }}