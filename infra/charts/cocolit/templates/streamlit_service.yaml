apiVersion: v1
kind: Service
metadata:
  name: {{ include "cocolit.fullname" . }}-streamlit-service
spec:
  selector:
    app: {{ .Values.streamlitService.selector.app }} 
  ports:
    - port: {{ .Values.streamlitService.ports.port }}         # Port exposed to the Ingress
      targetPort: {{ .Values.streamlitService.ports.targetPort }} # Port Streamlit is running on
  type: {{ .Values.streamlitService.type }}
