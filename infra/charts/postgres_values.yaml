auth:
  existingSecret: postgres-credentials
  username: cocolit_user
  database: cocolitdb
  secretKeys:
    adminPasswordKey: postgres-password
    userPasswordKey: password

postgresqlSharedPreloadLibraries: "postgis"

primary:
  initdb:
    scripts:
      postgis.sql: |
        CREATE EXTENSION IF NOT EXISTS postgis;